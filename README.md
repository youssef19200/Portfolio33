
  # Portfolio design preview

  This is a code bundle for Portfolio design preview. The original project is available at https://www.figma.com/design/nCQExeH1t7sp5FXh70Npfp/Portfolio-design-preview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  